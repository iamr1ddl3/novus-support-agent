# Normalize line endings to LF in repo.
# Working tree gets LF on all platforms (no auto-CRLF on Windows checkout).
* text=auto eol=lf

# Binary types: never touch
*.png binary
*.jpg binary
*.pdf binary
*.docx binary
*.xlsx binary
*.pptx binary
*.zip binary
